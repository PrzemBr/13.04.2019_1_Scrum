package pl.sda.academy.demo.exercises;

import java.text.SimpleDateFormat;
import java.time.LocalTime;

public class KalkulatorCzasu {

    public static String licz(String startTime, String endTime, String breakTime) {
        LocalTime timeToStart = LocalTime.parse(startTime);
        LocalTime timeToEnd = LocalTime.parse(endTime);
        LocalTime timeOfBreak = LocalTime.parse(breakTime);

        LocalTime time;
        return null;
    }
}
